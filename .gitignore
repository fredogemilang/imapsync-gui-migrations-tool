node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
.vscode/
.idea/
.turbo/
pnpm-lock.yaml.bak
apps/*/node_modules
apps/*/dist
apps/*/.turbo
data/
imapsync-state/

# playwright/manual test artefacts
.playwright-mcp/
*.png
!apps/web/public/*.png

# Design reference — not part of the runtime app
mockup/

# Per-developer Claude Code / IDE config
.claude/

# Local runbooks / notes that belong to other projects this user happens
# to keep alongside the repo. Not part of this project.
SSO-LLDAP-RUNBOOK.md
samples/

